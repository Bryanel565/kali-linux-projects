# Changelog

## 0.1.1 - 2025-12-19

- Rename project package to `library` and make packaging metadata reflect that.
- Added `library/` package with `Library` and CLI (`libman`).
- Added tests under `library/tests` and cleaned up old tests.
- Updated README, added installation and CLI usage instructions.
- Added compatibility shims under `Python/` to preserve backward compatibility.

## 0.1.0 - (previous)

- Initial implementation and learning exercises.
